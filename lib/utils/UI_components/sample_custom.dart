import 'package:flutter/material.dart';

class SampleCustomWidget extends StatelessWidget {
  const SampleCustomWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
